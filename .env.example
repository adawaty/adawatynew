# ── Supabase (required for contact form, pricing export & admin dashboard) ──
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ── Admin dashboard token ─────────────────────────────────────────────────────
# Any strong secret string — used to gate the /admin page
VITE_ADMIN_TOKEN=your-admin-token-here

# ── Legacy Neon (deprecated — Supabase is now the primary backend) ────────────
# DATABASE_URL=
# ADMIN_TOKEN=

# ── GitHub Actions (set in repo settings, not here) ──────────────────────────
# vars.NEON_PROJECT_ID=calm-moon-05727272
# secrets.NEON_API_KEY=...
